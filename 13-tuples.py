# Tuples
# zero-indexing
# can not directly modified (imutable)
# keyword: tuple()
tup = (1, True, "string")
count = tup.count(1)
index = tup.index("string")
contains = 1 in tup
print(tup)
print(count)
print(index)
print(contains)
